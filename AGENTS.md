# AGENTS.md

This is the neutral instruction entrypoint for any agent working in this repo.

Read these files in order:
1. `LOCAL_AI_INSTRUCTIONS.md` — workspace-specific context
2. `_context/CHARTER.md` — project definition and boundaries

If this repo uses Open Brain or another configured memory surface, follow that policy through the best compliant path available to this agent and declare blocked if no compliant path exists.

Agent-specific shims such as `CLAUDE.md` may add startup mechanics, but they do not replace the repo-local instruction stack above.
